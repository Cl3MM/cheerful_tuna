<div class="container">
  <div class="row-fluid">

    <div class="page-header">
      <h1>Delivery Request Form
        <% if current_user and current_user.is_admin? %>
          <span class="pull-left" style="margin-right: 20px;"><%= link_to "Edit template", "/editor" + request.path,
            class: "btn btn-primary",
            id: "edit_link",
            data: {url: mercury_update_html_snippet_path(HtmlSnippet.find_by_name("delivery_request_help"))} %></span>
        <% end %>
        </h1>
      </div>

      <%= render 'form' %>
    </div>
  </div>
