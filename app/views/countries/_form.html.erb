<%= simple_form_for(@country) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :english %>
    <%= f.input :french %>
    <%= f.input :chinese %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
